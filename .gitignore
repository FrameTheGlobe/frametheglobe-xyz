node_modules
.next
.DS_Store
dist
coverage
npm-debug.log*
yarn.lock
pnpm-lock.yaml
*.log

# generated static asset copy (produced by npm run build, served by Apache)
# never commit — stale files here cause /_next/static/ 404s after a rebuild
public/_next/

# deploy artifacts
deploy.env
deploy.sh
deploy-package.tar.gz

# local env (never commit secrets)
.env.local

# test artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

